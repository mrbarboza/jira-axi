{
  "name": "jira-axi-oauth-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Backend token-exchange proxy for jira-axi's shared Atlassian OAuth app (ADR-0003). Holds CLIENT_SECRET server-side; the CLI never sees it.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
